# 💱 Exchange Rate API Dashboard

A web application built with **Streamlit** and **ExchangeRatesAPI.io** to visualize real-time currency exchange rates.  
This project fetches live exchange rate data, processes it into a DataFrame, and creates visual analytics and downloadable outputs.

---

## 📊 Features
- Fetches **real-time currency exchange data** using ExchangeRatesAPI.io  
- Allows manual base currency conversion (since free API supports EUR only)  
- Displays data as an interactive DataFrame  
- Generates **bar chart visualizations** of top currencies  
- Exports results to **CSV** and **PNG**  

